<svg width="320" height="{{height}}" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-labelledby="cardTitle" role="img">
  <title id="cardTitle">Now playing on Jellyfin</title>
  <foreignObject width="320" height="{{height}}">
    <style>
      div {
        font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji;
      }

      .container {
        display: flex;
        align-items: center;

        border-radius: {{border_radius}}px;
        background: #{{background_color}};

        padding: 10px 10px
      }

      .cover-link {
        height: 64px;
      }

      .cover {
        border-radius: 1px;
        object-fit: cover;
        margin-right: 10px;
      }

      .playing {
        display: flex;
        justify-content: center;
        align-items: center;

        color: #00A4DC;
        font-weight: bold;
        text-align: center;

        margin-bottom: 8px;
      }

      .not-play {
        color: #ff1616;
        text-align: center;

        margin-bottom: 0;
      }

      .artist {
        color: #f4f4f5;
        font-weight: 600;
        font-size: 14px;

        margin-bottom: 3px;
      }

      .song {
        color: rgba(244, 244, 245, 0.62);
        font-size: 14px;

        margin-bottom: 18px;
      }

      #bars {
        position: absolute;
        height: 6px;
        width: 220px;
        overflow: hidden;
        margin: -6px 0 0 0px;
      }

      .bar {
        background: #{{bar_color|safe}};
        bottom: 1px;
        height: 3px;
        position: absolute;
        width: 3px;
        animation: sound 0ms -800ms linear infinite alternate;
        {% if is_paused %}animation-play-state: paused;{% endif %}
      }

      @keyframes sound {
        0% {
          opacity: .35;
          height: 3px;
        }

        100% {
          opacity: 1;
          height: 6px;
        }
      }

      {{css_bar|safe}}
    </style>
    <div xmlns="http://www.w3.org/1999/xhtml" class="container">
      {% if song_name %}
        {% if cover_image %}
          <a href="{}" target="_BLANK" class="cover-link">
            <img src="data:image/png;base64, {{img}}" width="64" height="64" class="cover" />
          </a>
        {% endif %}
        <div class="text-container">
          <div class="artist">{{artist_name}}</div>
          <div class="song">{{song_name}}</div>
          <div id='bars'>{{content_bar|safe}}</div>
        </div>
      {% else %}
        <div class="playing not-play">Nothing playing on Jellyfin</div>
      {% endif %}

    </div>
  </foreignObject>
</svg>
